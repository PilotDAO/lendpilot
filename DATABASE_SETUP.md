# Database Setup Guide (Supabase)

Этот проект использует **Supabase PostgreSQL** для хранения предзагруженных данных о рынках, что значительно ускоряет загрузку графиков.

## 🚀 Быстрый старт

### 1. Создайте проект в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте бесплатный аккаунт (если еще нет)
3. Создайте новый проект
4. Дождитесь завершения инициализации (1-2 минуты)

### 2. Получите Connection String

1. В проекте Supabase перейдите в **Settings** → **Database**
2. Найдите секцию **Connection string**
3. Выберите **URI** и скопируйте строку подключения
4. Она выглядит так:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
   ```

### 3. Настройте переменные окружения

Добавьте в `.env.local`:

```env
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"
```

⚠️ **Важно**: Замените `[YOUR-PASSWORD]` на ваш пароль из Supabase.

### 4. Создайте миграции

```bash
# Сгенерируйте Prisma Client
npm run db:generate

# Создайте и примените миграции
npm run db:migrate
```

Это создаст таблицы `market_timeseries` и `asset_snapshots` в вашей БД.

### 5. Запустите синхронизацию данных

```bash
# Синхронизируйте данные для всех рынков
npm run sync:market-data
```

Это загрузит данные из Subgraph в БД (может занять несколько минут при первом запуске).

### 6. Проверьте работу

1. Запустите dev сервер: `npm run dev`
2. Откройте `http://localhost:3000/ethereum-v3`
3. График должен загружаться **мгновенно** (< 50ms вместо 2-5 секунд)

## 📊 Структура базы данных

### Таблица `market_timeseries`

Хранит агрегированные данные по рынкам для графиков:

- `marketKey` - ключ рынка (например, "ethereum-v3")
- `date` - дата (YYYY-MM-DD)
- `window` - временное окно ("30d", "6m", "1y")
- `totalSuppliedUSD` - общий supply в USD
- `totalBorrowedUSD` - общий borrowing в USD
- `availableLiquidityUSD` - доступная ликвидность в USD

### Таблица `asset_snapshots`

Хранит исторические снимки активов (для будущего использования):

- `marketKey` - ключ рынка
- `underlyingAsset` - адрес токена
- `date` - дата снимка
- Метрики: supply, borrow, APR, utilization, price и т.д.

## 🔄 Автоматическая синхронизация

### Вариант 1: Vercel Cron (рекомендуется для продакшена)

Создайте файл `vercel.json`:

```json
{
  "crons": [
    {
      "path": "/api/cron/sync-market-data",
      "schedule": "*/15 * * * *"
    }
  ]
}
```

Создайте API route `app/api/cron/sync-market-data/route.ts`:

```typescript
import { syncAllMarkets } from '@/lib/workers/market-data-sync';

export async function GET(request: Request) {
  // Проверка авторизации (Vercel Cron secret)
  const authHeader = request.headers.get('authorization');
  if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return new Response('Unauthorized', { status: 401 });
  }

  await syncAllMarkets();
  return Response.json({ success: true });
}
```

### Вариант 2: Локальный cron (для разработки)

```bash
# Добавьте в crontab (crontab -e)
*/15 * * * * cd /path/to/lendpilot && npm run sync:market-data
```

### Вариант 3: Ручной запуск

```bash
npm run sync:market-data
```

## 🛠️ Полезные команды

```bash
# Генерация Prisma Client
npm run db:generate

# Создание новой миграции
npm run db:migrate

# Открыть Prisma Studio (GUI для БД)
npm run db:studio

# Синхронизация данных
npm run sync:market-data
```

## 📈 Производительность

| Подход | Время загрузки | Нагрузка на API |
|--------|----------------|-----------------|
| Без БД (Subgraph) | 2-5 секунд | Высокая |
| С БД (предзагрузка) | < 50ms | Минимальная |

## 🔍 Мониторинг

### Проверка данных в БД

```bash
# Откройте Prisma Studio
npm run db:studio
```

Или через Supabase Dashboard:
1. Перейдите в **Table Editor**
2. Найдите таблицу `market_timeseries`
3. Проверьте количество записей и последние обновления

### Логи синхронизации

При запуске `npm run sync:market-data` вы увидите:
- ✅ Успешные синхронизации
- ⚠️ Предупреждения (нет данных)
- ❌ Ошибки

## 🚨 Troubleshooting

### Ошибка подключения к БД

```
Error: Can't reach database server
```

**Решение**: Проверьте `DATABASE_URL` в `.env.local` и убедитесь, что:
- Пароль правильный
- Проект Supabase активен
- IP не заблокирован (Supabase → Settings → Database → Connection pooling)

### Нет данных в БД

Если график все еще грузится медленно:
1. Проверьте, что синхронизация запущена: `npm run sync:market-data`
2. Проверьте данные в БД: `npm run db:studio`
3. Убедитесь, что данные есть для нужного `marketKey` и `window`

### Миграции не применяются

```bash
# Сбросьте БД (⚠️ удалит все данные!)
npx prisma migrate reset

# Или создайте новую миграцию
npx prisma migrate dev --name init
```

## 📝 Примечания

- **Бесплатный tier Supabase**: 500 MB БД, 2 GB bandwidth/месяц
- **Рекомендуемая частота синхронизации**: каждые 15 минут
- **Хранение данных**: рекомендуется хранить последние 365 дней

## 🔗 Полезные ссылки

- [Supabase Dashboard](https://app.supabase.com)
- [Prisma Docs](https://www.prisma.io/docs)
- [Vercel Cron](https://vercel.com/docs/cron-jobs)
