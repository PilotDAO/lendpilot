# 🚀 Быстрый старт: Настройка Supabase

## Шаг 1: Создайте проект в Supabase

1. Перейдите на [supabase.com](https://supabase.com) и войдите/зарегистрируйтесь
2. Нажмите **New Project**
3. Заполните:
   - **Name**: `lendpilot` (или любое другое)
   - **Database Password**: создайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
4. Нажмите **Create new project**
5. Дождитесь завершения инициализации (1-2 минуты)

## Шаг 2: Получите Connection String

1. В проекте Supabase перейдите в **Settings** → **Database**
2. Найдите секцию **Connection string**
3. Выберите вкладку **URI**
4. Скопируйте строку подключения (она выглядит так):
   ```
   postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
   ```
5. ⚠️ **Важно**: Замените `[YOUR-PASSWORD]` на пароль, который вы создали на шаге 1

## Шаг 3: Настройте переменные окружения

Добавьте в файл `.env.local` (создайте, если его нет):

```env
DATABASE_URL="postgresql://postgres.xxxxx:ВАШ_ПАРОЛЬ@aws-0-us-east-1.pooler.supabase.com:6543/postgres"
```

⚠️ **Безопасность**: Убедитесь, что `.env.local` в `.gitignore` (он уже там)

## Шаг 4: Создайте таблицы в БД

```bash
npm run db:migrate
```

При первом запуске Prisma спросит имя миграции - введите `init`.

## Шаг 5: Запустите синхронизацию данных

```bash
npm run sync:market-data
```

Это загрузит данные из Subgraph в БД (может занять 2-5 минут при первом запуске).

## Шаг 6: Проверьте работу

1. Запустите dev сервер: `npm run dev`
2. Откройте `http://localhost:3000/ethereum-v3`
3. График должен загружаться **мгновенно** (< 50ms)!

## ✅ Готово!

Теперь данные синхронизируются автоматически. Для продакшена настройте Vercel Cron (см. `DATABASE_SETUP.md`).

## 🔍 Проверка данных в БД

```bash
# Откройте Prisma Studio (GUI для БД)
npm run db:studio
```

Или через Supabase Dashboard → **Table Editor** → `market_timeseries`

## ❓ Проблемы?

См. раздел **Troubleshooting** в `DATABASE_SETUP.md`
